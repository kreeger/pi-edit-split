## Agent skills

### Issue tracker

Issues are tracked as local Markdown files in `docs/issues/`. See
`docs/agents/issue-tracker.md`.

### Triage labels

Use the triage labels defined in `docs/agents/triage-labels.md`.

### Domain docs

Uses a single-context layout. See `docs/agents/domain.md`.
