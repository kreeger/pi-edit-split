# Domain Docs

## Before exploring, read these

- `CONTEXT.md` at the repo root, or `CONTEXT-MAP.md` if it exists.
- `docs/adr/` for decisions touching the area being changed.

If these files do not exist, proceed silently. Create them lazily only when
domain terms or decisions need recording.

## Layout

This is a single-context repo:

- `/CONTEXT.md`
- `/docs/adr/`
- `/src/`
